// Prettier configuration file
// You can use comments and dynamic logic here

module.exports = {
  singleQuote: true, // Preferred in TypeScript/JS projects for consistency
  printWidth: 100, // Slightly wider than default (80), good for modern monitors
  trailingComma: 'all', // Helps with cleaner diffs on multi-line objects/arrays
  semi: true, // Enforces semicolons, avoids ASI pitfalls
  arrowParens: 'avoid', // Makes single-param arrow functions concise
};
